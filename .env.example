# Canon Potato receives all runtime settings from the Translator parent app via
# postMessage. No environment variables are required for the iframe integration.

# Optional note for developers:
# Run Translator on http://localhost:3000 and this game on http://localhost:3004,
# then register the game in Translator with:
# GAME_APPS=canon-potato=http://localhost:3004
